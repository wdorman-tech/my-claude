---
description: Answer a yes/no question. Gives a direct y/n up front, then the reasoning. Usage: /yn [question]
---

The user asked a yes/no question: $ARGUMENTS

Answer using this exact format:

1. First line: just **y** or **n** (lowercase, nothing else on the line)
2. Two blank lines
3. Then your explanation/reasoning

Example output:

y


Because the function already handles null checks at the boundary, adding another one here would be redundant.

---

Now answer the user's question in that format. Be direct. Keep the explanation concise.
